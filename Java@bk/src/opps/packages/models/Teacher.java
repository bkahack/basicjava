package opps.packages.models;

public class Teacher {
	public static void main(String [] args) {
	Student obj =new Student("Prashant");
		obj.hello(); //we can access default method on all classes inside its parents class which is models
}
}